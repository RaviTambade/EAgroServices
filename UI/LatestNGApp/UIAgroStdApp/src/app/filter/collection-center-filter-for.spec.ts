import { CollectionCenterFilterFor } from './collection-center-filter-for';

describe('CollectionCenterFilterFor', () => {
  it('should create an instance', () => {
    expect(new CollectionCenterFilterFor()).toBeTruthy();
  });
});
