import { Transporterinvoices } from './transporterinvoices';

describe('Transporterinvoices', () => {
  it('should create an instance', () => {
    expect(new Transporterinvoices()).toBeTruthy();
  });
});
