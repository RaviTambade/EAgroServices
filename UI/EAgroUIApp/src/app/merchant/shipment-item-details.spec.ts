import { ShipmentItemDetails } from './shipment-item-details';

describe('ShipmentItemDetails', () => {
  it('should create an instance', () => {
    expect(new ShipmentItemDetails()).toBeTruthy();
  });
});
