import { CorporatedetailsDirective } from './corporatedetails.directive';

describe('CorporatedetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new CorporatedetailsDirective();
    expect(directive).toBeTruthy();
  });
});
