import { PaymentTransferDetails } from './payment-transfer-details';

describe('PaymentTransferDetails', () => {
  it('should create an instance', () => {
    expect(new PaymentTransferDetails()).toBeTruthy();
  });
});
