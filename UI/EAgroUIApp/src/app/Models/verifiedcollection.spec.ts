import { Verifiedcollection } from './verifiedcollection';

describe('Verifiedcollection', () => {
  it('should create an instance', () => {
    expect(new Verifiedcollection()).toBeTruthy();
  });
});
