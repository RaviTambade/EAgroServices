import { Merchantcollectioncount } from './merchantcollectioncount';

describe('Merchantcollectioncount', () => {
  it('should create an instance', () => {
    expect(new Merchantcollectioncount()).toBeTruthy();
  });
});
