import { Transporterrevenue } from './transporterrevenue';

describe('Transporterrevenue', () => {
  it('should create an instance', () => {
    expect(new Transporterrevenue()).toBeTruthy();
  });
});
