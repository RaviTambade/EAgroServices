import { TransporterPayment } from '../transporter-payment';

describe('TransporterPayment', () => {
  it('should create an instance', () => {
    expect(new TransporterPayment()).toBeTruthy();
  });
});
