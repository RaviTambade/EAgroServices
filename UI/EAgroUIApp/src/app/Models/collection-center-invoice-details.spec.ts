import { CollectionCenterInvoiceDetails } from './collection-center-invoice-details';

describe('CollectionCenterInvoiceDetails', () => {
  it('should create an instance', () => {
    expect(new CollectionCenterInvoiceDetails()).toBeTruthy();
  });
});
