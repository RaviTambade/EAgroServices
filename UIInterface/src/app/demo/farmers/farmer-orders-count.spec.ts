import { FarmerOrdersCount } from './farmer-orders-count';

describe('FarmerOrdersCount', () => {
  it('should create an instance', () => {
    expect(new FarmerOrdersCount()).toBeTruthy();
  });
});
