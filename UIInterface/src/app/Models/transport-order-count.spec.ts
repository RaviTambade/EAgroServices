import { TransportOrderCount } from './transport-order-count';

describe('TransportOrderCount', () => {
  it('should create an instance', () => {
    expect(new TransportOrderCount()).toBeTruthy();
  });
});
